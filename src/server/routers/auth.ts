import { router } from "../trpc";

/**
 * Auth router — registration and login are now handled by Supabase Auth
 * directly on the client. This router is kept as a placeholder for any
 * future server-side auth utilities.
 */
export const authRouter = router({});
